package com.example.financysystem.presentation.screens.userScreen.state.contentState

enum class OperatorSelectedContent(val selectedContent: Int) {
    PROFILE(0),
    TRANSFERS(1),
    SALARY_PROJECT(2)
}